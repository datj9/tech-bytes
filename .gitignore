node_modules/
dist/
.astro/
package-lock.json
.env
.env.*
!.env.example
__pycache__/
*.pyc
.venv/
cdk.out/
# cdk.context.json is intentionally committed: it caches the Route53 hosted-zone
# lookup so `cdk synth` works in CI (preview.yml) without AWS credentials.
*.js.map
*.d.ts
# data/*.json — tracked for initial/fallback content
.DS_Store
